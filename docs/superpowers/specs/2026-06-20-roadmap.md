# Regime Radar — Roadmap (2026-06-20, updated 2026-06-22)

**Status:** Approved (ranking locked). Tier 0, Tier 1, Tier 1b, and the credibility-keystone work are **done** — see "Completed since 2026-06-20" below.
**Context:** State-of-project review + reprioritization. All tests green at last update (backend 192, frontend 122, `tsc -b` clean).

## Guiding principle

Regime Radar is a **credibility product**: its value is one trustworthy number (current regime + transition risk). The original problem was that the number was **self-undermining** — live transition scores reach ~76% but the reliability table's validated range (`max_evaluated_p`) caps at ~0.30, so the UI disclaimed the score as a "directional stress signal, not a calibrated probability."

That problem is now **architecturally resolved** by the two-zone honesty feature (below): the number is split into a *validated* calibrated-probability zone (p ≤ `max_evaluated_p`, the only state that shows a "%") and an *unvalidated* stress/severity zone (`stress_in_support` / `stress_out_of_support`) that shows a severity tier and historical-rank language — never a calibrated-looking probability. The product no longer contradicts itself; it tells the truth about which zone it's in.

Polish and distribution amplify whatever trust the core number has. So the core number came first — and now stands on honest footing.

## Completed since 2026-06-20

- **Tier 0 — Hygiene:** analogs.py bugfix committed; in-flight planning docs tracked.
- **Tier 1 — Retrain + eval history:** freshness-aware fetch infrastructure operational; `eval_history` populated; `max_evaluated_p` flows through schema → evaluation → reliability table.
- **Tier 1b — Don't let it re-rot:** per-source freshness guard before training (`SOURCE_SPECS`, per-source cadence + asof), with panel-end staleness as defense-in-depth; `StaleDataError(reasons)` surfaces why a refresh was blocked. Incremental refresh added for SPY, FRED/EMV, and VIX (fallback chain preserved).
- **Predictability-ceiling diagnostic** (read-only, persists nothing): pre-registered label-variant grid + builder, `ceiling_metrics` (max_validated_p, top-group rate, monotonicity), out-of-support extrapolation distance (`support_distance.py`, nn_z prong), and a ceiling-sweep orchestrator with a pre-registered branch verdict. This is what established the ~0.30 validated ceiling and the 0.60 extrapolation boundary that the two-zone design rests on.
- **Two-zone honesty (credibility keystone):** backend-owned typed `RiskReading` contract — three support-first `display_state`s (validated / stress_in_support / stress_out_of_support), version-stamped raw-score reference for stress percentiles, fixed severity-tier cutpoints (0.85 / 0.97 / 0.995), `analog_status` enum, version-mismatch guard. Wired into both current-state and scenario serving paths; frontend renders strictly off `display_state` with a structural no-`%`-above-validated guarantee. Reviewed and approved; backend 192 / frontend 122 / `tsc -b` clean.
  - **Retrain-ops note:** `scripts/build_raw_score_reference.py` MUST be re-run after any retrain so the raw-score reference is re-stamped to the new model version; otherwise stress percentile/tier are (correctly) suppressed on the version-mismatch guard.

## Ranked tiers

### Tier 0 — Hygiene (immediate) — ✅ DONE
- ~~Commit the `src/models/analogs.py` bugfix~~ — done.
- ~~Track the loose planning docs~~ — done.

### Tier 1 — Credibility keystone — ✅ DONE
- ~~Execute the retrain + reliability rebuild + eval history plan~~ — done (freshness-aware fetch + eval history + `max_evaluated_p` pipeline).
- **Plus** the two-zone honesty contract, which converts the validated/unvalidated boundary from a UI disclaimer into permanent serving architecture (see "Completed since 2026-06-20").

### Tier 1b — Don't let it re-rot — ✅ MOSTLY DONE
- ~~Freshness guard before training~~ — done (per-source cadence guard + panel-end defense-in-depth).
- **Remaining:** scheduled retrain *cadence* (automated trigger) is still manual — the guard now *blocks* stale training, but nothing yet *initiates* a retrain on a schedule. Also: re-running `build_raw_score_reference.py` should be folded into the retrain pipeline so the raw-score reference can never silently drift from the serving model.

### Tier 2 — Ops hardening (live public deployment)
- `POST /refresh-data` (`routes.py:63`) is unauthenticated and triggers a full external-fetch recompute → add auth + rate limit.
- CORS defaults to `["*"]` when `CORS_ORIGIN` unset (`main.py:27`) → lock the default.
- `/historical-state` (`routes.py:113`) recomputes features+labels per request over a user-controlled range → cache / bound it.

### Tier 3 — Product/UX depth (drafted, pure-frontend)
- ~~Scenario Explorer redesign — Probability Tripod replacing the saturating RiskRail, verdict block, presets~~ — shipped (ProbabilityTripod + verdict block live; Scenario Explorer also now renders the three-state `RiskReading`).
- ~~Scenario driver cards~~ — shipped (driver delta cards live).
- **Remaining UX depth:** Current State is intentionally fallback-only for `RiskReading` (no redesign yet) — a fuller Current State stress treatment is the natural next product step now that the contract exists.

### Tier 4 — Growth
- Alerts / shareable regime snapshots / embeds, news integration, additional asset classes.

## Execution spine
Tier 0 → Tier 1 → Tier 1b are complete, and the two-zone honesty keystone is shipped. **Next agreed path:** finish Tier 1b automation (scheduled retrain cadence + fold `build_raw_score_reference.py` into the retrain pipeline), then Tier 2 ops hardening before broadening distribution (Tier 4).
